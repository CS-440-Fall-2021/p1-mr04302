let gl, canvas, program;
let vertices;

let eye, at, up;
let left, right, bottom, topval, near, far;
let pitch, yaw, roll;
let speed;

let modelViewMatrix, projectionMatrix;
let modelViewMatrixLoc, projectionMatrixLoc;
let pitchRotationMatrix, yawRotationMatrix, rollRotationMatrix;
let rotationMatrix, rotationMatrixLoc;

let renderMode;
let shaderMode;


